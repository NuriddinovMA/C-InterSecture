genome_path = #path to genome fasta-file
genome_file = #name of genome file
contact_dir = #path to directory with matrix, HiC-Pro output
contact_file = #name of file with contact matrix
Resolution = #bin size
mappability = #threshold of bins mappability, bins with polyN > mappablity% are discarded
cover_threshold = #threshold of bin coverage by hi-c reads, cover% bins with the baddest read coverage are discarded
limit = #Limit of distance in Mb, if contact distance > limit? contacts are united into one group. 
statistic = #method of contacts analysis: "abs" - for absolute number or "prc" - for percentile
out_dir = #path to output directory
out_name = #name of output file